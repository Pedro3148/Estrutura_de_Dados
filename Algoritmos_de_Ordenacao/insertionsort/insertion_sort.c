#include <stdio.h>
#include "insertion_sort.h"

void swap(int *num1, int *num2) {
    int temp = *num1;
    *num1 = *num2;
    *num2 = temp;
}

void insertion_sort(int *array, int length) {
    int i, j, atual;
    atual = i
    for (j = i + 1; j < length; j++) {
        if (array[j] > array[atual]) {}
    }
}